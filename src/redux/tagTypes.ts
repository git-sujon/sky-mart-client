export enum tagTypes {
  auth = "auth",
  users = "users",
  products = "products",
  carts = "carts",
}

export const tagTypesList = [
  tagTypes.auth,
  tagTypes.users,
  tagTypes.products,
  tagTypes.carts,
];
